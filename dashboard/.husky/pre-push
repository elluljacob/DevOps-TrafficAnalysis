# Just create the file directly
echo 'cd dashboard

echo "Type check"
npx tsc --noEmit

echo "Full test suite"
npm run test:ci

echo "Dependency scan"
npm audit --audit-level=high

echo "Secret scan"
docker run --rm -v $(git rev-parse --show-toplevel):/path zricethezav/gitleaks protect --staged --source /path' > .husky/pre-push

chmod +x .husky/pre-push