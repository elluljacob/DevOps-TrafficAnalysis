services:
  app:
    container_name: dashboard-container
    build: .
    ports:
      - "3000:3000"
    environment:
      - LOG_TO_FILE=false
      - USE_MOCK_DATA=false
    volumes:
      - .:/app
      - /app/node_modules
      - ./logs:/app/logs
