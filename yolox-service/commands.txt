pip install boto3

export AWS_ACCESS_KEY_ID="your_access_key_here"
export AWS_SECRET_ACCESS_KEY="your_secret_key_here"

python3 dynamodb.py


note: in the edge run:
openssl enc -aes-256-cbc -pbkdf2 -in .env -out secrets.enc
    enter our password when asked
rm .env

-
write bash script:
    set -a
    source <(openssl enc -d -aes-256-cbc -pbkdf2 -in secrets.enc)
    set +a
    docker compose up -d